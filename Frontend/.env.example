# Backend API base URL
VITE_API_BASE_URL=http://localhost:5000/api

# Google OAuth client ID used by the frontend popup flow.
# Use the same OAuth client configured for the backend.
VITE_GOOGLE_CLIENT_ID=your-google-client-id
